package pages

import (
	"github.com/kerkenes/templ-ui/internal/ui/examples"
	"github.com/kerkenes/templ-ui/internal/ui/layouts"
)

// ExamplePreview serves a registry example as a standalone page, the target
// of BlockPreview iframes (shadcn's /view/{name} pendant).
templ ExamplePreview(name string, entry examples.RegistryEntry) {
	@layouts.BaseLayout("Preview: "+name, "") {
		@entry.Component
	}
}
