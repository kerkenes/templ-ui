package examples

import (
	"github.com/kerkenes/templ-ui/components/button"
	"github.com/kerkenes/templ-ui/components/icon"
	"github.com/kerkenes/templ-ui/components/item"
)

templ ItemIcon() {
	<div class="flex w-full max-w-lg flex-col gap-6">
		@item.Item(item.Props{Variant: item.VariantOutline}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
				@icon.ShieldAlert()
			}
			@item.Content() {
				@item.Title() {
					Security Alert
				}
				@item.Description() {
					New login detected from unknown device.
				}
			}
			@item.Actions() {
				@button.Button(button.Props{
					Size:    button.SizeSm,
					Variant: button.VariantOutline,
				}) {
					Review
				}
			}
		}
	</div>
}
