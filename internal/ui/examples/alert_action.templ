package examples

import (
	"github.com/kerkenes/templ-ui/components/alert"
	"github.com/kerkenes/templ-ui/components/button"
)

templ AlertAction() {
	@alert.Alert(alert.Props{Class: "max-w-md"}) {
		@alert.Title() {
			Dark mode is now available
		}
		@alert.Description() {
			Enable it under your profile settings to get started.
		}
		@alert.Action() {
			@button.Button(button.Props{Size: button.SizeXs}) {
				Enable
			}
		}
	}
}
