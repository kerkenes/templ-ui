package showcase

import (
	"github.com/kerkenes/templ-ui/components/timepicker"
	"time"
)

templ TimePickerSelectedTime() {
	<div class="w-full max-w-sm">
		@timepicker.TimePicker(timepicker.Props{
			Value: time.Now(),
		})
	</div>
}
