package showcase

import "github.com/kerkenes/templ-ui/components/datepicker"

templ DatePickerDisabled() {
	<div class="w-full max-w-sm">
		@datepicker.DatePicker(datepicker.Props{
			Disabled: true,
		})
	</div>
}
