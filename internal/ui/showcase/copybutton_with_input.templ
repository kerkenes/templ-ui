package showcase

import (
	"github.com/kerkenes/templ-ui/components/copybutton"
	"github.com/kerkenes/templ-ui/components/input"
)

templ CopyButtonWithInput() {
	<div class="flex items-center gap-2">
		@input.Input(input.Props{
			ID:       "api-key-input",
			Value:    "abc123_example_key_xyz789",
			Readonly: true,
			Class:    "max-w-xs",
		})
		@copybutton.CopyButton(copybutton.Props{
			TargetID: "api-key-input",
		})
	</div>
}
